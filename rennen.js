/* var b = (100);

function makeAlert(){ 

};

function Timer(){
    document.getElementById("timer").value = `${b}`;
}

function counter(){
    while (b > 0){
        setTimeout(b - 1,0.1);
        console.log(b);
    }
}
setInterval(makeAlert, 10000);
Timer(); */